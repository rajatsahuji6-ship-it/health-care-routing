This directory stores trained DQN model weights.

Run training to generate:
    cd rl && python train.py

Output files:
    dqn_healthcare.pth      ← model weights
    dqn_healthcare_log.json ← training metrics
